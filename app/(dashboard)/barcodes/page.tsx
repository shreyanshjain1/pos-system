"use client"
import React from 'react'
import Button from '@/components/ui/Button'
import { Card } from '@/components/ui/Card'

export default function BarcodesPage() {
  return (
    <div>
      <div style={{ marginBottom: 18 }}>
        <h2 style={{ margin: 0 }}>Barcodes</h2>
        <p className="muted">Category feature removed — barcode workflow is in use.</p>
      </div>

      <Card>
        <div className="muted">This page is a placeholder. Categories have been removed from the product flows.</div>
      </Card>
    </div>
  )
}
