"use client"
import React from 'react'

// Outbox modal removed — keep a harmless stub so imports don't break.
export default function OutboxModal(_props: { open: boolean; onClose: () => void }) {
  return null
}
